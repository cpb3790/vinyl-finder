# Vinyl Finder Frontend (PWA)

## Local run
From `frontend/`:

```bash
python -m http.server 5173
```

Open:
- http://localhost:5173

### Backend override
Default backend is `http://localhost:8000`.

Override via:
- http://localhost:5173/?backend=https://YOUR_BACKEND_URL
